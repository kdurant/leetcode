cmake_minimum_required(VERSION 3.10)
set(projectName leetcode)
project(${projectName})

# add_subdirectory(cpp)
# add_subdirectory(doc/syntax)
# add_subdirectory(doc/tool)
add_subdirectory(std_algo)

# add_executable(${projectName} test.cpp)
